apiVersion: v1
kind: Service
metadata:
  name: {{ include "openwebui-9router-bridge.fullname" . }}
  labels:
    {{- include "openwebui-9router-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openwebui-9router-bridge.selectorLabels" . | nindent 4 }}
